# Copie para .env.local e preencha (não commite .env.local)

SUPABASE_URL=
SUPABASE_ANON_KEY=

# Login: só e-mail + senha no front. CAPTCHA opcional (Cloudflare Turnstile):
# no painel Supabase → Authentication → Attack Protection, e no browser em supabase-config.js:
# AURA_TURNSTILE_SITE_KEY (ver supabase/AUTH_CAPTCHA_INSTRUCOES.txt).
