# Copie para .env.local e preencha (não commite .env.local)

SUPABASE_URL=
SUPABASE_ANON_KEY=

# Login Google (painel Supabase → Authentication → Providers → Google):
# em "Redirect URLs", inclui a URL absoluta de login.html (ex.: https://teu-site.github.io/login.html).
