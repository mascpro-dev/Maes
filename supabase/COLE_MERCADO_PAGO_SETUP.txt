-- NÃO colar isto no SQL Editor como migração. Para SQL de tabelas usa:
-- supabase/migrations/20260410190000_mercado_pago_checkout_intents.sql
--
-- =============================================================================
-- MERCADO PAGO — forma simples (o botão “Ir pagar…” depende disto)
-- =============================================================================
--
-- Pix: a app mostra QR / Copia e Cola (API POST /v1/orders). Cartão: redirect Checkout Pro.
-- Aplica também: supabase/migrations/20260421120000_checkout_intents_mp_order_id.sql (coluna mp_order_id).
--
-- IMPORTANTE: SQL + secrets NÃO publicam a Edge Function.
-- Se a API responder NOT_FOUND / "Requested function was not found", a função
-- ainda não foi deployada. Usa o passo D ou o script deploy-mp-functions.cmd.
--
-- PASSO A PASSO (ordem sugerida)
--
-- A) SQL no Supabase (SQL Editor → New query)
--    Cola e executa SÓ o ficheiro:
--    supabase/migrations/20260410190000_mercado_pago_checkout_intents.sql
--
-- B) Credenciais Mercado Pago (conta onde queres receber o dinheiro)
--    1. Abre: https://www.mercadopago.com.br/developers/panel/app
--    2. Credenciais de produção (ou “Teste” para experimentar sem cobrar).
--    3. Copia o **Access Token** (começa por APP_USR-… ou TEST-…).
--    Não precisas da “Public Key” nesta integração: o checkout abre no site
--    do Mercado Pago; o token secreto vai só no Supabase (passo C).
--
-- C) Secrets no Supabase (liga o MP ao teu projeto)
--    1. Supabase Dashboard → teu projeto → **Project Settings** → **Edge Functions** → **Secrets**
--    2. Adiciona:
--       Nome: MERCADOPAGO_ACCESS_TOKEN     Valor: (cola o Access Token do passo B)
--       Nome: APP_PUBLIC_URL               Valor: https://maes-pi.vercel.app
--       (troca o URL se o teu site for outro; tem de ser https e sem barra no fim)
--
-- D) Publicar as funções (obrigatório — sem isto o pagamento dá sempre erro)
--    Opção 1 — duplo clique no Windows (na pasta do repo):
--       supabase\deploy-mp-functions.cmd
--    Opção 2 — PowerShell na pasta MaesAtipicas:
--       npx supabase login
--       npx supabase functions deploy mercadopago-create-preference --project-ref ahjhjzdmkkrcgbuxmhww --no-verify-jwt
--       npx supabase functions deploy mercadopago-webhook --project-ref ahjhjzdmkkrcgbuxmhww --no-verify-jwt
--    (Se o project-ref for outro, vê em Supabase → Settings → API → URL do projeto)
--
-- E) Opcional no código (Vercel / domínio fixo)
--    Em supabase-config.js podes preencher:
--    window.AURA_APP_PUBLIC_URL = 'https://maes-pi.vercel.app';
--    Isto reforça o domínio enviado ao servidor no pagamento.
--
-- F) Webhooks (para a consulta aparecer logo após pagar)
--    URL (produção, troca o ref se for outro projeto):
--    https://ahjhjzdmkkrcgbuxmhww.supabase.co/functions/v1/mercadopago-webhook
--    No painel MP → Suas integrações → Webhooks → Configurar notificações:
--      • Evento **Order (Mercado Pago)** — obrigatório para Pix (Orders / QR).
--      • Evento **Payment** — mantém o fluxo do cartão (Checkout Pro).
--    Podes usar a mesma URL para os dois eventos.
--
-- Se algo falhar, a mensagem vermelha na app tenta dizer o motivo (token, URL,
-- migração em falta, função sem deploy, etc.).
--
-- Erro UNAUTHORIZED_UNSUPPORTED_TOKEN_ALGORITHM (JWT / ES256):
-- O gateway do projeto ainda está a validar o JWT antes da função. Solução:
--   1) Volta a publicar com a flag (obrigatório): deploy-mp-functions.cmd
--      (usa --no-verify-jwt nas duas funções), OU na linha de comando os dois
--      deploy com --no-verify-jwt como no passo D acima.
--   2) No Dashboard: Edge Functions → cada função → desliga "Verify JWT" se existir.
-- O config.toml do repo também tem verify_jwt = false; a função valida o utilizador
-- com getUser() no código.
--
-- =============================================================================
-- Detalhe técnico (referência)
-- =============================================================================
--
-- - O valor R$ 49,90 está fixo na Edge Function (unit_price 49.9 BRL).
-- - Pix + cartão crédito 1x; débito e boleto “ticket” excluídos na preferência.
-- - Dinheiro cai na conta Mercado Pago do mesmo Access Token.
